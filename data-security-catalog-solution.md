# 数据字典到安全目录盘点方案

## 1. 文档状态

- 状态：方案基线
- 适用场景：保险数据字典到安全目录的批量分类分级
- 分类对象：数据字典中的字段
- 分类依据：安全目录（来源于《金融数据安全+数据安全分级指南》附录 A）
- 分级范围：当前只区分“敏感”和“非敏感”
- 当前方案：已完成架构决策，人工审核页面和部分参数待后续设计

## 2. 目标与范围

### 2.1 目标

对数据字典中的每个字段，自动确定：

```text
主题域
二级分类
三级分类
最细安全目录节点
敏感/非敏感
置信度分数
是否需要业务查看
```

系统同时支持两类实验：

1. 将完整安全目录提供给模型，观察准确率和消耗。
2. 先由程序召回候选目录，再让模型从候选中选择，观察节省的消耗以及召回损失。

还要比较两种字段提交方式：

1. 多个字段批量提交。
2. 一个字段逐条提交。

### 2.2 不在当前范围内的内容

当前不做以下事情：

- 不建立“数据资产到安全目录”的映射。
- 不单独维护一份重复的 `revised_paths`。
- 不把已有资产盘点结果当作标准答案。
- 不让模型独立决定敏感/非敏感。
- 不让置信度 AI 自动修改分类结果。
- 暂不设计完整的人工审核页面和审核流转系统。

## 3. 已确定的核心原则

### 3.1 安全目录只有一套正式分类体系

安全目录主数据直接维护附录 A 的正式分类路径，并为每个最细节点设置稳定的 `path_id`。

```text
path_id
主题域
二级分类
三级分类
最细目录名称
目录定义（有则维护）
适用范围（有则维护）
排除范围（有则维护）
敏感性：敏感 / 非敏感
目录版本
```

不维护一份与正式目录重复的 `revised_paths`。如果将来需要兼容旧名称，再增加独立的历史名称或映射表，不改变正式目录主表。

### 3.2 分类和分级绑定

模型只负责选择最细安全目录节点，敏感性由该节点的配置自动派生。

```text
字段信息
  -> 最细安全目录 path_id
  -> 程序读取该节点的 sensitivity
  -> 输出敏感或非敏感
```

例如：

```text
客户 / 个人 / 个人基本信息 / 联系方式
  -> 敏感
```

模型不得返回一个与最终路径不一致的敏感性，也不得把目录配置为敏感的节点改成非敏感。

### 3.3 历史盘点结果只能作为辅助证据

现有盘点信息可能不准确，其中的置信度也是模型生成的参考信息。因此它被单独放在“历史盘点参考”区域：

```text
当前字段信息
整表业务画像
安全目录或候选目录
历史盘点参考
```

历史结果可以帮助模型理解方向，但不能：

- 直接覆盖当前分类；
- 排除其他安全目录候选；
- 作为最终标准答案；
- 被当成真实概率使用。

如果历史结果与当前结果冲突，应记录冲突并提高业务查看优先级。

## 4. 输入数据与标识

### 4.1 原始数据字典字段

当前流程使用以下原始字段：

```text
主题域名称（修订后）
二级分类（修订后）
三级分类（修订后）
数据资产
系统名称
源系统名称说明
源表英文名
源表中文名
源字段英文名
源字段中文名
```

其中前三个“修订后”分类和历史置信度属于已有盘点参考，不属于安全目录主数据。

### 4.2 表级标识

整表画像按 `table_id` 缓存。建议使用以下信息共同确定表：

```text
系统名称 + 源系统名称说明 + 源表英文名
```

不能只使用源表英文名，因为不同系统可能存在同名表。

### 4.3 字段级标识

每个字段需要一个稳定的 `field_id` 或 `row_id`，用于：

- 将模型结果写回原始数据；
- 区分同名字段；
- 记录重试和版本；
- 比较不同实验方案的结果。

## 5. 总体流程

```text
原始数据字典
      |
      v
基础质检（已有能力）
      |
      v
按 table_id 分组
      |
      v
每张表生成一次整表业务画像并缓存
      |
      +------------------------------+
      |                              |
      v                              v
完整目录方案                    候选目录方案
      |                              |
      |                        程序召回候选路径
      |                              |
      +--------------+---------------+
                     v
            字段批量或逐条分类
                     |
                     v
             选择最细目录 path_id
                     |
                     v
             程序校验模型输出
                     |
                     v
       根据 path_id 派生敏感/非敏感
                     |
                     v
              置信度 AI 独立判断
                     |
                     v
       低分、冲突或异常时完整目录兜底
                     |
                     v
            输出结果和全过程记录
```

## 6. 流程说明

### 6.1 基础质检

基础质检已经在前置流程中完成，当前方案不再强制增加独立的清洗节点。

可以在整表画像阶段顺便生成辅助性的标准名称：

```text
原始字段名：CUST_TEL
原始中文名：客户联系电话（新）
标准化字段名：客户联系电话
```

要求：

- 原始名称必须保留；
- 标准名称不能覆盖原始值；
- 标准名称只帮助模型理解，不能直接决定安全目录；
- 标准名称推断失败不应阻断后续分类。

### 6.2 整表业务画像

以一张表的完整数据字典为输入，每张表只调用一次模型并缓存画像，供该表所有字段复用。

画像至少包含：

```text
业务对象
表的业务用途
一行数据代表什么
业务流程或生命周期阶段
可能涉及的数据主体
候选业务主题
可能相关的安全目录方向
推断依据
画像置信度
不确定点
```

整表画像的作用：

- 提供字段所处的业务语境；
- 帮助理解“状态、日期、编号、标志”等歧义字段；
- 提高候选目录召回率；
- 让同一张表的字段使用一致的表级背景。

整表画像不能硬性限制字段候选范围。即使画像判断为客户表，表中也可能同时有客户信息、保单信息、缴费信息和操作人员信息。

画像高置信度且没有明显矛盾时直接继续；画像低置信度、表名和字段集合冲突、历史盘点明显冲突时，记录表级风险，但暂不强制拦截字段分类。

### 6.3 历史盘点参考

历史盘点信息以明确标注的参考区域传入：

```json
{
  "historical_inventory": {
    "domain": "客户",
    "level2": "个人",
    "level3": "个人基本信息",
    "asset": "客户联系电话",
    "confidence": 0.82,
    "note": "历史模型结果，仅作辅助参考，可能错误"
  }
}
```

模型应优先依据当前字段和表语义；历史结果只用于补充方向和发现冲突。

### 6.4 候选目录召回

候选方案由程序召回，不由模型先自由创造候选。召回综合使用：

```text
字段中文名
字段英文名
数据资产
源表中文名
源表英文名
整表业务画像
同表字段语义
历史盘点结果
```

召回方式采用混合策略：

```text
关键词匹配
同义词和业务别名
向量相似度
整表画像扩展
历史盘点辅助加权
规则扩展
```

历史盘点只能提高某条路径的优先级，不能删除其他可能路径。

候选数量动态调整：

```text
明确字段：约 3～5 条
普通字段：约 5～10 条
状态、日期、编号、标志等模糊字段：约 10～20 条
高风险或有冲突字段：扩大候选范围
```

每次召回必须记录：

```text
候选路径列表
候选数量
每条候选的召回依据
候选排名
正确路径是否在候选中（仅金标准评估时可知）
```

### 6.5 字段分类

字段分类有两种输入组织方式，用于对比实验：

```text
批量方式：一张表作为上下文，一批字段同时提交
逐条方式：一张表作为上下文，一个字段单独提交
```

无论哪种方式，单个字段都必须具备：

```text
当前字段的完整信息
整表基本信息
整表完整字段清单
整表业务画像
历史盘点参考
完整目录或候选目录
```

批量提交时，每个字段仍然独立判断。模型可以参考同批字段理解表语义，但不能因为字段在同一张表中就强制使用同一个安全目录。

例如同一张客户表中可能出现：

```text
客户姓名   -> 个人基本信息
联系电话   -> 联系方式
保单号     -> 保单信息
缴费金额   -> 缴费信息
操作员工号 -> 员工或操作审计信息
```

### 6.6 模型选择最细目录

模型必须返回附录 A 的最细目录节点，而不是只返回主题域或三级目录。

候选目录以完整路径提供：

```json
{
  "path_id": "SEC-00128",
  "path": ["客户", "个人", "个人基本信息", "联系方式"],
  "definition": "描述个人联系电话、电子邮箱、联系地址等信息",
  "exclusions": "不包括银行账户和身份认证凭证"
}
```

模型一次完成完整路径选择，程序负责检查：

- `path_id` 是否存在；
- 返回路径和 `path_id` 是否一致；
- 最细节点是否属于对应三级分类；
- 三级分类是否属于对应二级分类；
- 二级分类是否属于对应主题域；
- 路径各层级是否来自同一条目录链。

### 6.7 敏感性派生

分类结果通过程序派生敏感性：

```text
最终 path_id
      |
      v
读取安全目录主表的 sensitivity
      |
      v
输出敏感或非敏感
```

当前不引入公开、脱敏、汇总等例外覆盖规则；如果以后增加这些输入，应另行设计程序化覆盖规则，不能让模型自由覆盖目录敏感性。

### 6.8 程序校验、重试和兜底

模型输出先经过程序校验。

技术性错误处理：

```text
JSON 格式错误       -> 尝试自动修复或重试 1～2 次
字段结果缺失         -> 携带错误信息重试
path_id 不存在       -> 要求仅从原目录重新选择
候选目录为空         -> 走完整目录方案
重试仍失败           -> 待业务查看
```

业务性不确定不应通过重试强行消除。例如字段语义确实模糊时，保留结果并标记需要查看。

必须区分以下状态：

```text
分类结果不确定
系统调用失败
程序格式校验失败
候选召回为空
完整目录兜底后仍失败
```

### 6.9 置信度 AI

置信度 AI 是独立模型节点，优先使用与分类 AI 不同的模型。它的职责不是重新分类，而是评价当前分类结果是否可靠。

```text
分类 AI：选择最细安全目录
置信度 AI：评价该选择是否有充分证据
```

置信度 AI 第一轮不提供分类 AI 的分类理由，避免被理由带偏。它看到：

```text
原始字段信息
整表字段信息
整表业务画像
历史盘点参考
分类 AI 选中的路径
分类 AI 实际使用的完整目录或候选目录
目录定义和排除范围
```

它输出：

```json
{
  "confidence_score": 82,
  "review_required": false,
  "reason": "字段名、表名和整表画像均支持当前路径",
  "suggested_path": null
}
```

如果证据不足：

```json
{
  "confidence_score": 43,
  "review_required": true,
  "reason": "字段名称过于通用，存在多个合理目录",
  "suggested_path": "客户/个人/个人基本信息/联系方式"
}
```

约束：

- 置信度 AI 可以给出疑似替代路径，但不能自动覆盖分类结果；
- 置信度 AI 使用分类 AI 实际看到的同一目录范围；
- 置信度 AI 不单独返回另一套分类结果；
- 原始分类模型分数可以留在日志中，但业务主要看置信度 AI 的分数；
- 业务展示 `0～100` 分数，不强制展示“高、中、低”等级；
- 程序内部可以依据分数阈值决定是否需要业务查看。

实验阶段对所有字段调用置信度 AI，用于评估它的价值；生产阶段再根据调优结果决定是否只对低分或冲突字段调用。

### 6.10 置信度阈值

置信度分数先作为审核参考分，不直接解释为真实正确概率。

阈值使用人工确认的调优集确定，而不是拍脑袋设置：

```text
测试 90 分以上自动通过的效果
测试 85 分以上自动通过的效果
测试 80 分以上自动通过的效果
```

每个阈值都观察：

```text
自动通过比例
自动通过结果的真实准确率
敏感数据漏判率
业务查看比例
```

先满足敏感数据漏判的安全底线，再尽量提高自动通过比例。

## 7. 两类实验方案

### 7.1 实验变量

实验只改变以下变量：

```text
目录提供方式：完整目录 / 候选目录
字段提交方式：批量 / 逐条
```

其他条件必须一致：

```text
模型版本
分类提示词版本
置信度提示词版本
安全目录版本
目录语义补充版本
整表画像版本
历史盘点输入
字段顺序
批次划分
生成参数
```

### 7.2 建议实验组合

至少运行：

```text
A：完整目录 + 字段批量
B：候选目录 + 字段批量
C：完整目录 + 字段逐条
D：候选目录 + 字段逐条
```

置信度 AI 在实验阶段对每个组合全量调用，并分别记录置信度 AI 的成本和效果。

### 7.3 重复运行

每种组合在金标准数据上重复运行 3 次，固定模型、提示词、目录、参数、字段顺序和批次划分。

统计：

```text
平均准确率
平均敏感性准确率
平均敏感漏判率
平均置信度分数
平均业务查看比例
平均 Token 消耗
平均耗时
运行结果波动
```

## 8. 金标准与评估

### 8.1 金标准建立

现有盘点结果不能直接作为标准答案。应从测试集中抽取样本，由人工确认：

```text
最细安全目录路径
敏感/非敏感（由目录路径确认）
是否存在特殊例外
人工确认依据
```

建议一人完成初标，第二人复核重点样本和随机样本，分歧由业务专家裁决。

### 8.2 数据集拆分

金标准按整表分组拆分：

```text
调优集：分析错误、修改提示词和召回规则
盲测集：方案定型后做最终比较
```

同一张表的字段不能拆到两个集合中，避免整表画像和相似字段造成信息泄漏。

样本应覆盖：

```text
语义明确字段
必须依靠整表才能理解的字段
状态、日期、编号、标志等模糊字段
历史盘点与当前结果冲突的字段
相近目录容易混淆的字段
敏感和非敏感边界字段
不同系统和业务域的表
```

### 8.3 评价指标

第一层是安全底线：

```text
敏感 -> 非敏感的漏判率
高风险分类错误率
非法路径率
```

第二层是分类效果：

```text
最细目录准确率
主题域准确率
二级分类准确率
三级分类准确率
敏感/非敏感准确率
```

第三层是运营成本：

```text
自动通过比例
业务查看比例
平均每张表成本
平均每个字段成本
输入 Token
输出 Token
调用次数
处理耗时
```

### 8.4 候选召回专项指标

候选方案要区分两类错误：

```text
正确路径已经召回，但模型选错
正确路径根本没有被召回
```

在金标准集上记录：

```text
正确路径是否进入 Top 3
正确路径是否进入 Top 5
正确路径是否进入 Top 10
候选平均数量
候选方案最终准确率
候选方案节省的 Token
完整目录兜底比例
```

### 8.5 方案选择规则

不能用单一综合分数直接选择方案。应按以下顺序：

```text
先淘汰敏感漏判超过安全底线的方案
再比较最细目录准确率
准确率接近时比较业务查看比例和成本
结果波动明显时优先选择更稳定的方案
```

## 9. 输出结果设计

最终结果必须保留全过程信息，不只保存最终分类。

### 9.1 业务原始信息

```text
field_id
table_id
系统名称
源系统名称说明
源表英文名
源表中文名
源字段英文名
源字段中文名
数据资产
```

### 9.2 表画像信息

```text
表画像内容
表画像版本
表画像置信度
表画像风险标记
标准化表名（如有）
标准化字段名（如有）
```

### 9.3 分类结果

```text
分类方案
分类提交方式
path_id
主题域
二级分类
三级分类
最细目录名称
敏感/非敏感
分类模型原始置信度
分类模型理由（如有）
候选路径列表
```

### 9.4 置信度 AI 结果

```text
confidence_score
review_required
confidence_reason
suggested_path
分类 AI 与置信度 AI 是否冲突
```

### 9.5 运行和审计信息

```text
模型版本
分类提示词版本
置信度提示词版本
安全目录版本
目录语义补充版本
召回规则版本
输入 Token
输出 Token
调用次数
重试次数
处理耗时
系统处理状态
```

人工审核功能虽然暂缓，但结果表应预留：

```text
是否人工确认
人工最终路径
人工修改原因
审核人
审核时间
```

## 10. 生产策略

### 10.1 初期生产或试运行

```text
整表画像
  -> 候选目录召回
  -> 字段批量分类
  -> 程序校验
  -> 置信度 AI 全量判断
  -> 低分、冲突字段走完整目录兜底
  -> 输出分类、敏感性和置信度分数
```

初期全量调用置信度 AI，是为了积累人工确认数据并校准分数。

### 10.2 稳定运行后

```text
明确且高分结果       -> 自动通过
低分结果             -> 置信度 AI 或业务查看
分类方案冲突         -> 完整目录兜底
候选为空             -> 完整目录兜底
多次运行不一致       -> 提高查看优先级
```

字段逐条提交作为实验和特殊表的处理方式，不作为默认生产方式。

### 10.3 版本发布与回滚

模型、提示词、目录、目录定义、召回规则、画像逻辑和审核阈值都必须版本化。

发布流程：

```text
修改组件
  -> 调优集验证
  -> 固定回归集验证
  -> 盲测集验证
  -> 小批量试运行
  -> 检查安全指标、准确率、查看比例和成本
  -> 逐步扩大生产范围
```

生产结果不能被新版本直接覆盖。需要保留新旧结果和版本，以支持回滚和差异分析。

## 11. 目录语义知识管理

附录 A 原始内容作为标准层，不允许模型修改。每个目录节点的补充信息单独维护：

```text
standard_*：附录 A 原始内容
curated_*：人工确认的定义、适用范围、排除范围和示例
generated_*：模型生成的草稿，不能直接投入正式分类
```

如果附录 A 没有完整定义，第一阶段不强行补写。后续可以人工确认：

```text
目录定义
适用范围
排除范围
典型字段示例
容易混淆的相邻目录
```

## 12. 当前暂缓事项

以下内容尚未展开设计，不应在实现时自行假设：

```text
人工审核页面和人员流转流程
业务查看后的批量修改方式
置信度 AI 的具体分数计算提示词
置信度阈值的具体数值
候选召回的具体关键词、向量模型和数据库选型
数据字典和结果表的具体存储技术
```

这些事项应基于本方案的固定边界继续逐项设计。
