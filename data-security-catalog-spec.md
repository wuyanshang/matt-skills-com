# 数据字典到安全目录盘点技术规格

## Problem Statement

当前需要对保险数据字典进行批量安全目录盘点。每条输入记录描述一个源字段，但字段名称可能存在歧义，单独看字段往往无法判断其业务含义。现有资产盘点结果可以提供方向，但其中的主题域、二级分类、三级分类、数据资产和置信度均可能不准确，不能直接作为安全分类标准。

现有流程还缺少可重复的实验框架，无法客观回答以下问题：

- 完整安全目录和候选目录哪种分类方式更准确、更省成本；
- 字段批量提交和逐条提交哪种方式更适合生产；
- 历史盘点信息加入模型上下文后究竟是帮助还是误导；
- 模型输出的分类结果哪些可以自动接受，哪些需要业务查看；
- 分类错误来自候选召回、模型选择、目录定义还是上下文组织；
- 提示词、模型、目录和召回规则变化后，如何回归评估和回滚。

## Solution

建立一条可配置、可重复、可审计的数据字典到安全目录分类流水线。

流水线以“字段”为最终分类对象，以“表”为业务上下文边界：

1. 复用已有基础质检结果；
2. 按系统、源系统说明和源表英文名形成 `table_id`；
3. 每张表只生成一次整表业务画像并缓存；
4. 将历史盘点信息按稳定字段键关联，并通过开关控制是否发送给模型；
5. 并行支持完整目录和程序召回候选目录两种模式；
6. 并行支持字段批量和逐条两种提交模式；
7. 让分类模型一次选择附录 A 的最细安全目录节点；
8. 由程序根据最终 `path_id` 派生敏感/非敏感；
9. 使用独立的置信度 AI 评估分类可靠性并输出 0～100 分；
10. 对低分、冲突、候选为空或兜底触发条件的记录走完整目录兜底；
11. 保留输入、目录、模型、提示词、召回、校验、重试和置信度全过程记录；
12. 在人工金标准上比较准确率、安全漏判率、业务查看比例、Token、耗时和稳定性。

正式安全目录只有一套，以附录 A 的分类路径为标准，并为每个最细节点设置稳定 `path_id`。不单独维护重复的 `revised_paths`，也不建立当前范围外的“数据资产到安全目录”映射。

## User Stories

1. 作为数据安全盘点人员，我希望对每个数据字典字段得到最细安全目录路径，以便形成安全目录盘点结果。
2. 作为数据安全盘点人员，我希望同时得到主题域、二级分类、三级分类和最细节点，以便检查路径完整性。
3. 作为数据安全盘点人员，我希望敏感/非敏感由最终安全目录自动派生，以便避免模型独立分级造成分类与分级不一致。
4. 作为数据安全盘点人员，我希望保留源表和源字段原始名称，以便追溯模型使用的证据。
5. 作为数据安全盘点人员，我希望同一张表只生成一次整表画像，以便所有字段使用一致的表级背景并降低调用成本。
6. 作为数据安全盘点人员，我希望整表画像描述业务对象、表用途和一行数据含义，以便帮助模型理解状态、日期、编号和标志等歧义字段。
7. 作为数据安全盘点人员，我希望整表画像只能提供上下文而不能强制所有字段采用同一分类，以便允许一张表包含多个业务对象或数据类型。
8. 作为数据安全盘点人员，我希望已有盘点结果被明确标为历史参考，以便模型不会把可能错误的旧结果当成标准答案。
9. 作为实验人员，我希望能够切换“带历史盘点信息”和“不带历史盘点信息”，以便测量历史信息的真实收益。
10. 作为实验人员，我希望两种历史模式使用同一份历史数据和关联规则，以便只比较是否发送历史信息这一变量。
11. 作为实验人员，我希望能够运行完整安全目录方案，以便建立覆盖完整目录的基线。
12. 作为实验人员，我希望能够运行候选目录方案，以便测量候选召回带来的成本节省和召回损失。
13. 作为实验人员，我希望能够冻结候选集进行对照，以便区分历史信息对模型选择和对候选召回的影响。
14. 作为实验人员，我希望能够比较字段批量提交和逐条提交，以便评估上下文共享、干扰、成本和稳定性的差异。
15. 作为实验人员，我希望同一字段在不同实验组合中使用相同的表画像、字段顺序和批次边界，以便保证比较公平。
16. 作为实验人员，我希望每种方案重复运行三次，以便观察模型随机性和结果波动。
17. 作为实验人员，我希望知道正确路径是否被候选召回，以便区分召回错误和模型选择错误。
18. 作为实验人员，我希望知道历史信息帮助了多少字段、伤害了多少字段，以便判断是否值得把历史信息设为默认输入。
19. 作为实验人员，我希望按历史记录质量分层统计结果，以便知道高置信度历史和低置信度历史的不同影响。
20. 作为模型调用程序，我希望模型输出结构化的 `path_id` 和完整路径，以便程序校验父子关系并写回原始字段。
21. 作为模型调用程序，我希望模型输出缺失、格式错误或非法路径时能够有限重试，以便减少可修复的技术失败。
22. 作为模型调用程序，我希望候选为空时自动转入完整目录，以便避免因召回失败而编造分类。
23. 作为数据安全盘点人员，我希望语义确实不明确的字段被标记为需要业务查看，以便不把不确定结果伪装成确定结论。
24. 作为置信度评估人员，我希望置信度 AI 独立检查分类结果，而不是重新覆盖分类，以便明确分类责任和评估责任。
25. 作为置信度评估人员，我希望置信度 AI 第一轮看原始证据而不看分类模型理由，以便降低被模型解释带偏的风险。
26. 作为业务人员，我希望看到一个 0～100 的置信度分数和原因，以便按分数筛选需要查看的记录。
27. 作为业务人员，我希望置信度 AI 可以提供疑似替代路径但不能自动替换结果，以便保留人工判断空间。
28. 作为业务人员，我希望能够区分分类模型、置信度 AI、程序校验和系统调用失败，以便正确理解异常原因。
29. 作为审计人员，我希望每条结果带有模型、提示词、目录、召回规则和运行版本，以便重现历史结果。
30. 作为审计人员，我希望保留旧版本和新版本结果，以便进行差异分析和回滚。
31. 作为评估人员，我希望使用人工确认的金标准而不是历史盘点结果，以便测量真实分类准确率。
32. 作为评估人员，我希望调优集和盲测集按整表拆分，以便避免表级上下文泄漏。
33. 作为评估人员，我希望安全指标优先于成本指标，以便避免因节省调用而增加敏感数据漏判。
34. 作为生产运维人员，我希望新版本先小批量试运行，再逐步放量，以便降低上线风险。
35. 作为生产运维人员，我希望发现问题后可以回滚到旧版本，以便恢复稳定的分类流程。

## Implementation Decisions

### 1. 领域对象与主数据

- `CatalogPath`：安全目录最细节点，包含稳定 `path_id`、完整层级路径、定义、适用范围、排除范围、敏感性和目录版本。
- `TableContext`：一张源表的原始信息、完整字段清单、`table_id` 和缓存的整表业务画像。
- `FieldRecord`：待分类字段及其 `field_id`、所属 `table_id`、原始数据字典字段和可选标准名称。
- `HistoricalInventory`：历史盘点分类、数据资产、历史置信度、关联状态和历史版本；它是参考输入，不是安全目录主数据。
- `RunConfig`：一次实验或生产运行的不可变配置，至少包括目录模式、提交模式、历史模式、模型版本、提示词版本、目录版本、召回规则版本、数据集版本和重复运行序号。

### 2. 表级画像边界

- 以 `系统名称 + 源系统名称说明 + 源表英文名` 确定 `table_id`。
- 每张表只生成一次画像并缓存；表名、表说明或字段集合发生实质变化时生成新画像版本。
- 画像输出业务对象、表用途、一行数据含义、业务阶段、数据主体、候选业务方向、推断依据、不确定点和画像置信度。
- 可在画像阶段生成标准化表名和字段名，但原始值必须保留，标准名称不能直接决定安全目录。
- 画像是辅助证据，不能限制字段候选范围，也不能让同表字段自动继承同一分类。

### 3. 历史信息关联与开关

- 历史关联优先使用 `field_id`；没有时依次使用 `table_id + 源字段英文名`、`table_id + 源字段中文名`。
- 无法唯一匹配时标记 `ambiguous`，不得强行选择一条历史记录。
- 关联状态使用 `matched`、`unmatched`、`ambiguous`。
- 请求配置提供 `history_mode`：`with_history` 或 `without_history`。
- `without_history` 请求不发送历史字段，也不发送“没有历史信息”的说明文字，避免上下文差异污染对照。
- 历史盘点结果及其原始置信度始终保存在系统记录中，但不得覆盖当前分类、不得排除其他候选、不得作为金标准。

### 4. 目录提供与候选召回

- 完整目录模式向分类模型提供全部正式路径。
- 候选目录模式由程序使用关键词、同义词、向量相似度、整表画像、同表字段语义和历史辅助加权进行召回。
- 历史信息可以提高候选优先级，但不能删除其他候选。
- 候选数量随字段歧义动态调整：明确字段约 3～5 条，普通字段约 5～10 条，状态/日期/编号/标志等模糊字段约 10～20 条；冲突或高风险字段可扩大范围。
- 每次召回保存候选列表、排名、候选数量、召回依据和召回规则版本。
- 主对照实验先冻结候选集，单独比较历史信息对模型选择的影响；随后再做历史参与召回的端到端专项实验。

### 5. 分类模型接口

- 输入包含当前字段、整表基本信息、整表完整字段清单、整表画像、历史参考（按开关）、完整目录或候选目录以及运行配置标识。
- 批量模式一次提交多个目标字段，但要求每个字段独立判断；逐条模式每次只提交一个目标字段。
- 模型一次选择完整最细路径，不拆成多次主题域、二级、三级调用。
- 输出至少包含 `field_id`、`path_id`、完整路径、备选路径、分类理由、使用的证据字段和模型原始分数。
- 模型不能创建目录中不存在的名称，不能输出不完整路径，也不能独立覆盖敏感性。

### 6. 程序校验与结果派生

- 校验 `path_id` 是否存在、路径名称是否一致、父子层级是否合法、字段是否遗漏或重复、输出结构是否完整。
- JSON 或字段缺失等技术错误允许有限重试；路径非法时携带校验错误要求模型仅从原候选重新选择。
- 候选为空时转完整目录；重试仍失败或语义不确定时标记业务查看，不编造分类。
- 通过最终 `path_id` 读取目录主数据中的敏感性，程序派生“敏感/非敏感”。
- 当前不实现公开、脱敏、汇总等例外覆盖；未来增加时必须使用程序规则，不交给模型自由覆盖。

### 7. 置信度 AI 接口

- 置信度 AI 是独立模型节点，优先使用不同于分类模型的模型。
- 它不重新分类、不自动修改分类结果，只评价当前路径是否有充分证据。
- 第一轮不提供分类模型理由，只提供原始证据、整表画像、历史参考、当前分类结果、实际目录范围和目录定义。
- 使用与分类模型相同的目录范围：完整模式看完整目录，候选模式看同一候选集。
- 输出 `confidence_score`（0～100）、`review_required`、原因和可选的疑似替代路径。
- 业务主要查看置信度 AI 分数；分类模型自报分数可以留在审计日志中。
- 置信度分数先作为审核参考分，不直接解释为真实正确概率；阈值用人工确认的调优集校准。
- 实验阶段对所有字段调用置信度 AI；生产阶段根据调优结果决定是否只调用低分或冲突字段。

### 8. 实验设计

实验变量为：

- 目录模式：完整 / 候选；
- 提交模式：批量 / 逐条；
- 历史模式：带历史 / 不带历史。

基础组合共 8 组。资源有限时先运行完整目录和候选目录下的批量带历史/不带历史 4 组，再运行逐条组合。

所有对照固定模型、提示词、目录、目录语义、表画像、字段顺序、批次边界、历史数据集、关联规则和生成参数。每组在金标准调优数据上重复运行 3 次。

历史专项对同一 `field_id` 做成对比较并计算：

- `history_helped`：不带历史错误，带历史正确；
- `history_harmed`：不带历史正确，带历史错误；
- `history_no_effect`：两者相同；
- `history_changed`：路径发生变化。

### 9. 金标准与指标

- 金标准由人工确认最细安全目录，敏感性由该目录派生；历史盘点结果不能直接充当标准答案。
- 初标由一人完成，重点样本、冲突样本和随机样本由第二人复核，分歧由业务专家裁决。
- 调优集和盲测集按整表分组，不能拆分同一张表的字段。
- 安全底线指标优先：敏感到非敏感漏判率、非法路径率、高风险错误率。
- 分类效果指标：最细目录、主题域、二级、三级和敏感性准确率。
- 运营指标：自动通过比例、业务查看比例、每表/每字段成本、输入输出 Token、调用次数、耗时和波动。
- 候选专项指标：正确路径 Top 3/5/10 召回率、候选平均数量、候选最终准确率和完整目录兜底率。
- 先淘汰安全底线不达标方案，再比较准确率；准确率接近时比较业务查看比例、成本和稳定性。

### 10. 结果与审计记录

每条结果保留原始字段、表画像、历史匹配状态、分类方案、提交方式、历史模式、候选路径、最终 `path_id`、派生敏感性、置信度 AI 分数、查看标记、原因、替代路径、模型输出、重试记录和全过程版本。

每张表和每个字段都必须能通过 `table_id`、`field_id` 和 `run_id` 关联到同一次运行。运行配置不可在处理过程中隐式修改。

### 11. 生产与发布

- 初期默认候选目录 + 字段批量 + 置信度 AI 全量调用。
- 低分、冲突、候选为空或兜底触发时使用完整目录。
- 稳定运行后，再根据调优结果缩小置信度 AI 调用范围。
- 历史模式保持可切换；只有历史帮助率高于伤害率、敏感漏判不升高且收益覆盖成本时，才将带历史设为默认。
- 模型、提示词、目录、目录语义、画像逻辑、召回规则和阈值全部版本化。
- 新版本先调优集、固定回归集、盲测集，再小批量试运行和逐步放量；保留旧版本以支持回滚。

## Testing Decisions

测试只验证外部行为和可观察结果，不绑定具体模型 SDK、向量数据库或内部类名。

### 必测模块

1. 表级分组和画像缓存：同一 `table_id` 只生成一次画像；表标识变化会生成新版本。
2. 历史信息关联：稳定键优先级正确；重复匹配标记为 `ambiguous`；无匹配标记为 `unmatched`。
3. 历史模式隔离：`without_history` 请求不包含历史内容；`with_history` 请求包含匹配历史内容。
4. 候选召回：候选数量策略、召回依据记录、候选为空的完整目录兜底。
5. 分类输出校验：非法 `path_id`、父子层级不一致、字段遗漏和重复输出均被拦截或重试。
6. 敏感性派生：敏感性始终来自最终目录主数据，不采信模型返回值。
7. 置信度 AI：使用与分类模式相同的目录范围；不修改分类；输出分数、原因和查看标记。
8. 批量与逐条：结果均能按 `field_id` 正确回写，批量字段不会被错误合并为同一分类。
9. 实验记录：8 组配置、重复序号、Token、耗时和版本信息完整可追溯。
10. 成对评估：同一字段的带历史/不带历史结果能够正确计算帮助、伤害和无影响类别。
11. 版本和回滚：新旧运行结果并存，指定版本可重现或重新运行受影响字段。

### 评估测试

- 使用人工确认调优集验证阈值与真实错误率的关系。
- 使用盲测集比较完整目录、候选目录、批量、逐条、带历史和不带历史组合。
- 每种组合重复三次，报告均值、最小值、最大值和波动。
- 对敏感漏判进行单独统计，不能被总体准确率掩盖。
- 对候选召回先测召回率，再测模型在候选中的选择准确率。
- 对历史信息按高/低历史置信度、冲突和无历史分层统计。

## Out of Scope

- 当前不建立数据资产到安全目录的映射。
- 当前不单独维护重复的 `revised_paths`。
- 当前不实现完整人工审核页面、人员分派、批量修改和审批流。
- 当前不实现公开、脱敏、汇总等敏感性例外覆盖。
- 当前不指定具体模型厂商、模型 SDK、向量数据库、关键词库或持久化技术。
- 当前不把模型自报分数解释为经过校准的真实概率。
- 当前不允许置信度 AI 自动改写分类结果。
- 当前不以历史盘点结果作为金标准。

## Further Notes

第一阶段建议先实现最小可比较闭环：

```text
完整目录 + 批量 + 带历史
完整目录 + 批量 + 不带历史
候选目录 + 批量 + 带历史
候选目录 + 批量 + 不带历史
```

先固定候选集，回答历史信息是否帮助模型选择；之后再放开历史参与召回，回答历史信息是否帮助候选召回。逐条模式用于第二阶段对比提交粒度。

生产初期先全量调用置信度 AI，不急于根据分数自动放行。待调优集积累足够人工确认结果后，再确定分数阈值和是否缩小调用范围。

人工审核流程虽然暂缓，但结果表需要预留人工确认字段，保证后续人工结果能够回写为新的金标准和错误案例，而不是直接无版本地覆盖模型结果。
