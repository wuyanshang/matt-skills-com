# 数据字典到安全目录：分层召回与敏感字段专项保护

## 1. 文档目的

当前安全目录召回已经包含短查询、长查询、画像查询、向量检索和 BM25 检索，但端到端效果仍受到两个问题影响：

1. 字段名称和安全目录名称经常不是同义词。例如“角色代码”可能对应“个人标签信息”，直接按字段名称检索很难命中。
2. 一级、二级目录名称本身比较宽泛。如果先错误地确定一个一级或二级分支，再只在该分支下找四级目录，正确路径会在早期被过滤掉。

当前还存在安全风险：候选方案的总体效果可能提高，但敏感数据漏判率上升。因此需要增加敏感字段专项保护，优先保证敏感字段的候选覆盖和最终分级安全性。

本文只讨论两个方面：

```text
一、一级、二级模糊时如何做分层召回
二、敏感字段如何扩大候选或回退完整目录
```

## 2. 总体原则

### 2.1 分层是召回组织方式，不是硬过滤

分层召回不能变成下面这种流程：

```text
先选一个一级分类
  -> 先选一个二级分类
    -> 只在这个分支下召回四级目录
```

因为一级、二级名称通常比较宽泛，早期选错会导致正确四级路径永远无法出现。

正确做法是软分层：

```text
召回多个可能的一级/二级分支
  -> 汇总这些分支下的四级叶子
  -> 加入全局补充候选
  -> 对四级候选统一重排
  -> 交给分类模型选择
```

一级、二级只用于提高候选排序和组织候选，不直接决定最终路径。

### 2.2 安全保护优先于平均准确率

敏感字段专项保护不能只看总体四级准确率。方案必须同时满足：

```text
敏感字段正确路径具有足够的候选覆盖
敏感字段进入模型 Top 15 的比例足够高
敏感 -> 非敏感的最终漏判率不高于安全基线
```

如果候选方案节省了 Token，但敏感漏判率明显上升，就不能直接作为默认生产方案。

### 2.3 最终敏感性仍由目录路径派生

敏感专项保护中的“敏感风险”只是召回和兜底信号，不是最终分级结果。

```text
敏感风险识别
  -> 扩大候选或完整目录兜底
  -> 分类模型选择最终 path_id
  -> 程序从 path_id 读取敏感/非敏感
```

不能因为预筛选判断为“疑似敏感”，就直接把字段标成敏感；最终分级仍然来自正式目录节点。

## 3. 目录主数据准备

分层召回的关键不是只给每个节点加一个向量，而是为每个分支和叶子建立可检索的语义资料。

### 3.1 叶子目录文档

每条四级叶子目录至少形成以下文本：

```text
完整路径
一级分类
二级分类
三级分类
四级目录名称
四级定义
三级定义说明
上级目录说明
安全级别和最低安全级别参考
备注
人工确认同义词
典型字段示例
易混淆字段
排除范围
```

例如“个人标签信息”不能只建立如下文档：

```text
个人标签信息
```

应建立为类似下面的语义文档：

```text
路径：客户 / 个人 / 个人基本信息 / 个人标签信息
定义：描述个人标签、角色、人员类型、客户分类、身份属性等信息
可能字段：角色代码、角色名称、人员类型、客户标签、客户分类、身份标识
相关概念：个人角色、业务角色、用户角色、客户标签、人员标签、角色编码
排除：证件号码、联系方式、银行账户、交易金额
```

其中人工确认的定义、同义词和示例进入正式语义层；模型生成的扩展词只能作为待确认草稿。

### 3.2 分支语义文档

一级或二级目录本身定义宽泛时，为每个分支生成聚合文档。聚合内容来自该分支下的所有叶子，但要保留叶子路径信息：

```text
branch_id
分支路径
分支定义
下属三级和四级目录名称
下属目录定义摘要
下属目录典型字段
下属目录同义词
排除范围
```

例如“个人基本信息”分支的检索文本不应只包含“个人基本信息”，还应包含：

```text
姓名、性别、国籍、婚姻状况、证件类型、证件号码、家庭住址、联系方式、个人标签、个人角色、人员类型等
```

分支文档用于召回，不用于最终分类。最终分类仍然落到四级叶子。

### 3.3 文档类型与归并主键

建议在 ES 中区分：

```text
branch：一级/二级分支语义文档
category：四级叶子目录主文档
example：字段和业务示例文档
alias：人工确认的别名和同义词文档
```

所有文档都保留：

```text
category：归并到四级叶子的完整路径
branch_id：所属分支
doc_type：文档类型
```

示例、别名和分支命中后，都通过 `category` 归并到正式叶子，不把示例文档直接作为最终分类结果。

## 4. 分层召回方案

### 4.1 查询输入扩展

原始字段名不能直接作为唯一查询。建议为每个字段构造多个查询视图：

```text
Q1：源字段中文名
Q2：数据资产 + 源字段中文名
Q3：源表中文名 + 源字段中文名
Q4：整表画像 + 源字段中文名
Q5：字段语义扩展文本
```

字段语义扩展可以由规则、同义词表或独立语义扩展 AI 生成。扩展结果必须保留原始字段名，不能覆盖原值。

例如：

```text
原始字段：角色代码
表名：客户信息表
同表字段：客户姓名、性别、证件号码、联系电话
```

语义扩展可以得到：

```text
业务对象：个人或客户
字段含义：个人在业务系统中的角色、身份或人员类型编码
相关概念：个人标签、客户标签、人员类型、个人角色、业务角色、角色编码
```

### 4.2 第一阶段：召回多个一级/二级分支

对分支文档分别执行向量检索和 BM25，使用 RRF 或归一化加权融合：

```text
branch_vector
branch_bm25
  -> 分数或排名归一化
  -> RRF / Weighted Rank Fusion
  -> 得到 Top M 个分支
```

建议初始参数：

```text
Top M 分支：5～10 个
高歧义字段：10～20 个
```

不要只保留分数最高的一个分支。分支候选至少要满足以下一种覆盖：

```text
向量路命中
BM25 路命中
字段语义扩展命中
整表画像命中
敏感风险保护命中
```

如果不同路由给出了不同分支，应保留这些分支的并集，再进入叶子召回。

### 4.3 第二阶段：在分支并集下召回四级叶子

根据 Top M 分支筛选四级叶子，但不能只依赖分支内检索。候选池由三部分组成：

```text
A：Top M 分支下的四级叶子
B：全目录直接召回的全局四级叶子
C：敏感保护规则补充的安全相关叶子
```

候选池：

```text
candidate_pool = A ∪ B ∪ C
```

其中 B 是防止分层路由早期误判造成漏召回的保险。建议初始配置：

```text
A：每个分支取 Top 10～30 个叶子
B：全局召回 Top 10～20 个叶子
C：按敏感风险追加安全相关叶子
```

### 4.4 分支覆盖配额

在候选池重排前设置分支覆盖配额，避免某个分支因分数偏高占满 Top 15：

```text
每个高排名分支至少保留 1～2 个叶子
每个重要召回路由至少保留 1 个叶子
敏感保护命中的分支必须至少保留 1 个叶子
```

配额只用于候选池构造，最终排序仍要使用候选语义得分。配额不能强行改变最终分类结果。

### 4.5 第三阶段：四级候选重排

候选池合并后执行统一重排。推荐顺序：

```text
多路召回
  -> 去重
  -> RRF 或归一化加权融合
  -> 分支覆盖修正
  -> 四级候选重排
  -> 取 Top 15 / Top 30 / Top 50 做实验
```

候选重排输入：

```text
字段中文名和英文名
数据资产
源表中文名和英文名
整表画像
候选完整路径
候选定义和说明
候选示例和同义词
候选召回路由及排名
历史盘点参考（如果当前实验开启）
```

重排可以先用规则和向量相似度完成，再用独立重排序模型做对照。重排序模型只改变候选顺序，不直接输出正式分类结果。

### 4.6 “角色代码”示例

输入：

```text
源字段中文名：角色代码
源表中文名：客户信息表
同表字段：客户姓名、性别、证件号码、联系电话
```

流程：

```text
字段扩展：个人角色、人员类型、客户标签、业务角色、角色编码
  -> 分支召回：个人信息、客户信息、组织人员信息等多个分支
  -> 叶子召回：个人标签信息、个人其他信息、人员身份信息等
  -> 全局补充：保留全局语义相近叶子
  -> 四级重排：结合表画像和同表字段
  -> 模型选择：个人标签信息
```

关键点是：

```text
“个人标签信息”不需要和“角色代码”文字完全相同；
目录定义、典型字段、同义词和表级上下文共同完成语义连接。
```

## 5. 敏感字段专项保护

### 5.1 专项保护的定位

敏感字段保护在分类前或分类后都可以触发，但它不直接决定最终敏感性。它只负责提高召回覆盖，降低敏感字段被错误降级的概率。

建议使用内部字段：

```text
sensitive_risk：none / possible / high
```

这个字段用于控制候选数量和回退策略，不直接写入最终结果中的“敏感/非敏感”。

### 5.2 敏感风险信号

风险识别采用高召回策略，信号来源包括：

```text
字段中文名
字段英文名
数据资产
源表中文名和表说明
整表业务画像
历史盘点分类和历史置信度
已召回候选的 sensitivity 配置
字段类型或字段名模式
```

可以维护人工确认的敏感关键词和概念组，例如：

```text
身份：姓名、证件、身份证、护照、国籍、婚姻
联系方式：电话、手机、邮箱、地址、住址
账户：银行卡、账户、账号、支付
交易：金额、缴费、保费、余额、收益
健康：疾病、诊断、医疗、健康
人员标签：角色、人员类型、客户标签、身份属性
```

这些词只用于提高召回和触发保护，不能直接替代目录分类。

### 5.3 敏感风险判定方式

建议采用“规则高召回 + 语义判断补充”的方式：

```text
规则命中明确敏感概念       -> possible
字段、表和资产组合命中      -> possible 或 high
整表画像涉及个人/客户且字段含义不清 -> possible
历史结果指向敏感节点        -> possible，但记录为弱信号
已召回敏感目录且排名靠前    -> high
无任何敏感证据              -> none
```

如果采用 AI 做风险判断，AI 只能输出风险信号和理由，不能直接给出最终敏感性。

### 5.4 候选充分性检查

分类模型运行前，程序检查候选是否足够。以下任一条件成立，就认为候选可能不足：

```text
候选数量低于最小值
候选全部来自同一路由
候选全部来自同一个狭窄分支
候选中没有任何敏感节点，但 sensitive_risk != none
Top 1 与 Top 2 的融合分数过于接近
向量、BM25、画像路由的结果冲突明显
候选在多次快照或重复运行中不稳定
历史盘点与当前字段语义冲突
置信度 AI 分数低于阈值
```

建议记录具体原因：

```text
candidate_insufficient_reason =
  low_count / no_sensitive_path / low_margin / route_conflict /
  branch_concentration / unstable / history_conflict
```

### 5.5 敏感字段的三级处理策略

#### 普通字段

```text
sensitive_risk = none
候选充分
  -> 使用常规 Top 15 候选
```

#### 疑似敏感字段

```text
sensitive_risk = possible
或候选充分性检查失败
  -> 扩大到 Top 30 / Top 50
  -> 增加敏感相关分支和定义示例
  -> 保留全局补充候选
  -> 调用置信度 AI
```

#### 高风险字段

```text
sensitive_risk = high
且出现低分、冲突、候选不足或敏感路径未进入 Top 15
  -> 先保存候选方案结果
  -> 重新使用完整安全目录分类
  -> 使用完整目录结果作为当前正式分类结果
  -> 记录 fallback_reason
```

完整目录回退的触发条件可以包括：

```text
敏感风险为 high 且 Top 15 没有敏感候选
置信度 AI 分数低于敏感字段专用阈值
分类 AI 与置信度 AI 对路径意见冲突
候选路径之间没有明显区分
候选召回多路结果分歧严重
候选方案把可能敏感字段分到非敏感节点
```

### 5.6 伪代码

```text
for field in fields:
    risk = detect_sensitive_risk(
        field=field,
        table_profile=profile,
        historical_inventory=history_if_enabled
    )

    candidates = retrieve_layered_candidates(
        field=field,
        table_profile=profile,
        history=history_if_enabled,
        include_global_residual=True
    )

    sufficiency = check_candidate_sufficiency(
        candidates=candidates,
        sensitive_risk=risk,
        route_scores=True,
        branch_coverage=True
    )

    if risk == "possible" or not sufficiency.ok:
        candidates = expand_candidates(
            candidates=candidates,
            target_size=50,
            add_sensitive_branches=True,
            add_definition_examples=True,
            preserve_branch_coverage=True
        )

    classification = classify_from_candidates(field, candidates)
    classification = validate_and_derive_sensitivity(classification)
    confidence = evaluate_confidence(field, profile, candidates, classification)

    need_fallback = (
        risk == "high"
        and (
            confidence.score < sensitive_threshold
            or confidence.conflict
            or not contains_sensitive_candidate(candidates)
            or classification.sensitivity == "非敏感"
        )
    )

    if need_fallback:
        full_result = classify_from_full_catalog(field, profile, history_if_enabled)
        final_result = validate_and_derive_sensitivity(full_result)
        final_result.fallback_reason = collect_reasons(
            risk, sufficiency, confidence, classification
        )
    else:
        final_result = classification

    persist_field_result(
        final_result=final_result,
        sensitive_risk=risk,
        candidate_sufficiency=sufficiency,
        confidence=confidence,
        fallback_used=need_fallback
    )
```

伪代码中的 `classification.sensitivity == 非敏感` 不是单独把结果改成敏感，而是高风险保护的触发信号：如果字段证据很可能涉及敏感信息，但候选分类得到非敏感，必须重新检查或回退完整目录。

### 5.7 敏感专项输出字段

每个字段至少保存：

```text
sensitive_risk
sensitive_risk_reasons
sensitive_signal_sources
candidate_count_before_expand
candidate_count_after_expand
candidate_insufficient
candidate_insufficient_reason
sensitive_candidate_in_top15
confidence_score
confidence_conflict
fallback_used
fallback_reason
candidate_path
full_catalog_path（如果发生回退）
final_path_id
final_sensitivity
```

候选结果和完整目录回退结果都要保留，不能只保存最后一条路径，否则无法分析保护策略是否有效。

## 6. 评估设计

### 6.1 分层召回指标

分层召回需要分别统计：

```text
一级分支召回率
二级分支召回率
四级正确路径全量召回率
四级正确路径 Top 15 / Top 30 / Top 50 命中率
分支覆盖率
候选平均数量
重排后正确路径排名
```

一级、二级只作为召回辅助，因此不能只看一级或二级命中。最终要看正确四级路径是否进入候选。

### 6.2 敏感字段专项指标

敏感专项至少单独报告：

```text
敏感字段候选召回率
敏感字段进入 Top 15 比例
敏感字段进入 Top 30 比例
敏感字段最终四级准确率
敏感字段敏感/非敏感准确率
敏感 -> 非敏感漏判率
敏感字段候选扩展触发率
敏感字段完整目录回退率
回退后敏感漏判率
回退带来的 Token 增量
```

重点比较四组：

```text
普通候选方案
候选方案 + 敏感字段扩展
候选方案 + 敏感字段扩展 + 完整目录回退
完整目录基线
```

### 6.3 保护策略的判断标准

敏感保护策略需要同时观察收益和代价：

```text
敏感候选召回率是否提高
敏感字段 Top 15 命中率是否提高
敏感漏判率是否下降
完整目录回退后是否恢复安全基线
额外 Token 和耗时是否可接受
误报和业务查看数量是否增加过多
```

如果候选扩展提高了召回率，但模型仍然经常选非敏感路径，问题就从召回转移到了候选排序或分类提示词；如果回退后仍然漏判，则应检查目录定义、金标准和分类模型，而不是继续扩大候选数量。

## 7. 推荐实施顺序

```text
1. 修复候选合并，使用 RRF 或归一化融合并重排
2. 重建候选快照，重新跑端到端基线
3. 建立分支文档和叶子语义文档
4. 实现软分层召回：Top M 分支 + 全局补充候选
5. 比较 Top 15 / Top 30 / Top 50 截断效果
6. 实现敏感风险识别和候选充分性检查
7. 实现敏感字段候选扩展
8. 实现敏感字段完整目录回退
9. 人工研判召回不到的语义鸿沟样本
10. 在调优集确定敏感阈值，再用盲测集验收
```

其中第 1、2 步必须先完成，因为未重排的旧候选集不能作为后续策略的可靠基线。第 6～8 步完成后，候选方案才具备作为生产默认方案的安全基础。
